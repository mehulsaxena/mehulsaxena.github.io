repos:
- repo: https://github.com/pre-commit/mirrors-prettier
  rev: "v2.7.1" # Match the prettier 2.x devDependency so pre-commit and npm run format agree
  hooks:
    - id: prettier
      files: ".*.(json|js|css)"


